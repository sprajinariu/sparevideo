CAPI=2:
name: "sparevideo:ip:window"
description: "Reusable 3x3 sliding-window primitive (line buffers + window regs + edge handling)"

filesets:
  files_rtl:
    files:
      - rtl/axis_window3x3.sv
    file_type: systemVerilogSource

targets:
  default:
    filesets:
      - files_rtl
